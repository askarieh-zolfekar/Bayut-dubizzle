/**
 * General Reducer
 */

/**
 * initial general values
 */
const INIT_STATE = {
};

export default (state = INIT_STATE, {type, payload }) => {
    switch (type) {
        default:
            return { ...state };
    }
}
